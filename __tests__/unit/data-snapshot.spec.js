'use strict';

const DataSnapshot = require('../../lib/data-snapshot');

let dataSnapshot;
describe('DataSnapshot testing suite', () => {
  beforeEach(() => {
    dataSnapshot = new DataSnapshot(data);
  });

  it('should', () => {

  });
});
